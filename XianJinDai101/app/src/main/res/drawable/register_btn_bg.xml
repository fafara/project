<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 内部颜色 -->
    <solid
        android:color="@android:color/white" />
    <!-- 圆角的幅度 -->
    <corners
        android:radius="3dp"/>
    <stroke
        android:color="@color/register_contract_text_color"
        android:width="1px"
       />

</shape>

